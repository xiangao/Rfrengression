# Ensure Input is a Torch Tensor

Ensure Input is a Torch Tensor

## Usage

``` r
as_tensor(x, dtype = torch_float())
```

## Arguments

- x:

  A matrix, data frame, vector, or torch tensor.

- dtype:

  Torch data type (default: torch_float()).

## Value

A torch tensor.
