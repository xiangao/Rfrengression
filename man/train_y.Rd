% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frengression.R, R/frengression_seq.R,
%   R/frengression_surv.R
\name{train_y}
\alias{train_y}
\alias{train_y.frengression}
\alias{train_y.frengression_seq}
\alias{train_y.frengression_surv}
\title{Train the Outcome Model (Y)}
\usage{
train_y(model, ...)

\method{train_y}{frengression}(
  model,
  x,
  z,
  y,
  num_iters = 100,
  lr = 0.001,
  print_every = 10,
  tol = 0.01,
  silent = FALSE,
  ...
)

\method{train_y}{frengression_seq}(
  model,
  s,
  x,
  z,
  y,
  num_iters = 100,
  lr = 1e-04,
  print_every = 10,
  silent = FALSE,
  ...
)

\method{train_y}{frengression_surv}(
  model,
  s,
  x,
  z,
  y,
  num_iters = 100,
  lr = 0.001,
  print_every = 10,
  reg_lambda = 0,
  silent = FALSE,
  ...
)
}
\arguments{
\item{model}{A frengression, frengression_seq, or frengression_surv object.}

\item{...}{Additional arguments passed to methods.}

\item{x}{Treatment matrix or tensor.}

\item{z}{Confounder matrix or tensor.}

\item{y}{Outcome matrix or tensor (events).}

\item{num_iters}{Number of training iterations (default: 100).}

\item{lr}{Learning rate (default: 1e-3).}

\item{print_every}{Number of iterations between progress prints (default: 10).}

\item{tol}{Early stopping tolerance (default: 0.01).}

\item{silent}{Suppress output (default: FALSE).}

\item{s}{Static baseline variables (matrix or tensor).}

\item{reg_lambda}{Regularization weight for marginal loss (default: 0).}
}
\value{
The model with updated weights and training loss history.
}
\description{
Jointly trains model_y (causal margin) and model_eta (association remainder).
Uses the Z-permutation trick to ensure eta becomes marginally N(0,I).
}
